import { Validations.Validator } from './validations.validator';

describe('Validations.Validator', () => {
  it('should create an instance', () => {
    expect(new Validations.Validator()).toBeTruthy();
  });
});
